function Multiplica1 (numero1: number, numero2: number) {
    return numero1 * numero2;
}

function DizOlá (nome: string) {
    return `${nome}` + `diz olá`;
}

console.log(Multiplica1(5, 8));
console.log(DizOlá("Vitor"));